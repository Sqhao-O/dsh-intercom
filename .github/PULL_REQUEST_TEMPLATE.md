## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] PR title follows Conventional Commits (`feat:`, `fix:`, `chore:`, ...)
- [ ] `pnpm lint && pnpm typecheck && pnpm test` pass locally
- [ ] If sources changed: `pnpm build` run and updated `lib/` committed
- [ ] User-facing change: changeset added (`pnpm changeset`)
- [ ] Vendored `broker/` changes documented in `NOTICE` (if any)

## Testing

<!-- How was this tested? Which OS / Node versions? -->
